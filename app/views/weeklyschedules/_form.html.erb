<%= simple_form_for @weeklyschedule do |f| %>
  <% if @weeklyschedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weeklyschedule.errors.count, "error") %> prohibited this weeklyschedule from being saved:</h2>

      <ul>
      <% @weeklyschedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :lessonhours_id %><br>
  </div>
  <div class="field">
    <%= f.input :day_id %><br>
  </div>
  <div class="field">
    <%= f.input :curriculum_id %><br>
  </div>
  <div class="field">
    <%= f.input :classroom_id %><br>
  </div>
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
